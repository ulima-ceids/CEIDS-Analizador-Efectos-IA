# Copy to .env.local and fill in your values

# URL of the FastAPI backend (leave unset to use mock)
VITE_API_URL=http://localhost:8000

# Set to "false" to disable mock and hit real backend
VITE_USE_MOCK=true
